import 'package:flutter/material.dart';

/// Chart colors — desktop only (admin dashboard).
abstract class AppChartColors {
  static const Color primaryLine = Color(0xFFF5A623);
  static const Color gridLine = Color(0xFF2A3A4E);
  static const Color label = Color(0xFF9CA3AF);
}
